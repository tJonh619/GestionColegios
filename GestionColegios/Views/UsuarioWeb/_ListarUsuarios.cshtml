@model GestionColegios.ViewModels.VMUsuario

<div class="card mt-3 shadow">
    <div class="card-header bg-primary text-white">
        <h3 class="card-title mb-0">Lista de Usuarios</h3>
    </div>
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-hover align-middle">
                <thead class="table-light">
                    <tr>
                        <th class="fs-5">@Html.DisplayNameFor(model => model.Usuarios.First().CodigoUsuario)</th>
                        <th class="fs-5">@Html.DisplayNameFor(model => model.Usuarios.First().NombreUsuario)</th>
                        <th class="fs-5">@Html.DisplayNameFor(model => model.Usuarios.First().ClaveHash)</th>
                        <th class="fs-5">@Html.DisplayNameFor(model => model.Usuarios.First().CorreoRecuperacion)</th>
                        <th class="fs-5">@Html.DisplayNameFor(model => model.Usuarios.First().FechaModificacion)</th>
                        <th class="fs-5">Nombre del rol</th>
                        <th class="fs-5">Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var usuario in Model.Usuarios)
                    {
                        <tr>
                            <td class="fs-5">@Html.DisplayFor(modelItem => usuario.CodigoUsuario)</td>
                            <td class="fs-5">@Html.DisplayFor(modelItem => usuario.NombreUsuario)</td>
                            <td class="fs-5">@Html.DisplayFor(modelItem => usuario.ClaveHash)</td>
                            <td class="fs-5">@Html.DisplayFor(modelItem => usuario.CorreoRecuperacion)</td>
                            <td class="fs-5">@Html.DisplayFor(modelItem => usuario.FechaModificacion)</td>
                            <td class="fs-5">@Html.DisplayFor(modelItem => usuario.Rol.Nombre)</td>
                            <td class="fs-5">
                                <a href="@Url.Action("Edit", new { id = usuario.Id })" class="btn btn-sm btn-warning me-2">
                                    <i class="bi bi-pencil-square"></i> <span class="fs-5">Detalles</span>
                                </a>
                                <a href="@Url.Action("Delete", new { id = usuario.Id })" class="btn btn-sm btn-danger px-4 py-2" onclick="return confirm('¿Estás seguro de que deseas eliminar este usuario?');">
                                    <i class="bi bi-trash3-fill"></i> <span class="fs-5">Desactivar</span>
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>